name: Keep Database Alive
on:
  schedule:
    - cron: '0 6 */5 * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health endpoint
        run: curl -f https://YOUR_BACKEND_URL/health || true
