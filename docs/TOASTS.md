# Toast Notification System

Location: lib/toast/ToastContext.tsx
Usage: const { showToast } = useToast(); showToast('message', 'success' | 'error' | 'info');
Auto-dismisses after 4 seconds. Rendered via a fixed bottom-right container in the root layout.
