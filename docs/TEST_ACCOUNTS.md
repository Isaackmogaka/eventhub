# Test Accounts

Use your own registered accounts for local testing. Roles are set via
Prisma Studio (npx prisma studio) by editing the User.role field directly —
there is no public endpoint to self-promote to ORGANIZER or ADMIN.
