# Shared Components Reference

- Sidebar — collapsible nav, props: active, onLogout, isAdmin, isOrganizer
- Skeleton / EventCardSkeleton — loading placeholders
- PasswordInput — reusable password field with show/hide toggle
- CommandPalette — Cmd/Ctrl+K navigation and event search
- LocationPicker / LocationDisplay — Leaflet map input and read-only display
